 1. Set the 'application.prefix' property in build/pom.xml

 1. Set the 'application.db-dialect' property in build/pom.xml
 1. Set the 'application.db-datasource' property in build/pom.xml
 1. Set the 'application.db-username' property in build/pom.xml
 1. Set the 'application.db-password' property in build/pom.xml

 1. [OPTIONAL] Set the 'application.lucene-index-path' property in build/pom.xml
 1. [OPTIONAL] Set the 'filestore.path' property in build/pom.xml

 1. [OPTIONAL] Set the 'application.name' property in build/pom.xml to <client>-<project>-<cccVersion>

 1. Run the build
 1. Change the name of the ear file to <application.name>.ear // TODO: automate this using 'finalName'

 1. Update the command line tools to specify the correct application name, db server, etc.