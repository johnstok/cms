targetApplicationURL=http://localhost:8080/${application.prefix}/creator
targetUploadURL=http://localhost:8080/${application.prefix}/creator/upload

app-name=${application.name}

# Connection parameters for the legacy database
sourceDbServerName=poseidon
sourceDbPortNumber=1521
sourceDbSID=DEV
sourceDbUsername=ccc_migration
sourceDbPassword=d3ccc_migration

# File system paths for the legacy files and images
filesSourcePath=files/
imagesSourcePath=images/
cssSourcePath=css/

link-prefix=/${application.prefix}/
