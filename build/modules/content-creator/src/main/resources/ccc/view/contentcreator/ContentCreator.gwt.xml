<module>

	<!-- Inherit the core Web Toolkit stuff. -->
	<inherits name="com.google.gwt.user.User" />
	<inherits name="com.google.gwt.json.JSON" />
	<inherits name="com.google.gwt.user.Debug" />
	<inherits name="com.google.gwt.i18n.I18N"/>

	<!-- Inherit the default GWT style sheet. -->
	<inherits name="com.google.gwt.user.theme.standard.Standard" />

	<!-- Supported locales. -->
	<extend-property name="locale" values="fi" />

	<!-- Specify the app entry point class. -->
	<entry-point class="ccc.view.contentcreator.client.ContentCreator" />

	<!-- RPC servlet bindings. -->
	<servlet path="/resource"
		class="ccc.remoting.gwt.ResourceServiceImpl" />

	<!-- Specify the application specific style sheet. -->
	<stylesheet src="ContentCreator.css" />

</module>
